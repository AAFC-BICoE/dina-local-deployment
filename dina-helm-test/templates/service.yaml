apiVersion: v1
kind: Service
metadata:
  name: {{ include "dina-helm-test.fullname" . }}
  labels:
    {{- include "dina-helm-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dina-helm-test.selectorLabels" . | nindent 4 }}
