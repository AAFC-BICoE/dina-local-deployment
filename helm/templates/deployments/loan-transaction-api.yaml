apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ .Values.app.name }}
  name: {{ .Values.app.name }}-{{ .Values.global.environment.config.loan_transaction_api.label }}
spec:
  replicas: {{ if and .Values.profiles.loan_transaction_api.enabled (not (or .Values.global.environment.config.db_init.restore_db .Values.global.environment.config.db_init.reset_users)) }}1{{ else }}0{{ end }}
  selector:
    matchLabels:
      app: {{ .Values.app.name }}-{{ .Values.global.environment.config.loan_transaction_api.label }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0  
  template:
    metadata:
      annotations: {}
      creationTimestamp: null
      labels:
        app: {{ .Values.app.name }}-{{ .Values.global.environment.config.loan_transaction_api.label }}
    spec:
      initContainers:
        {{- include "dina-helm.initContainer.checkDb" . | nindent 8 }}
        {{- include "dina-helm.initContainer.initDb" (dict "root" . "serviceKey" "loantransactionapi" "environmentKey" "loan_transaction_api" "shortName" "loan_transaction") | nindent 8 }}
      containers:
        - name: loan-transaction-api
          {{- with .Values.loantransactionapi.image }}
          image: {{ include "dina-helm.build-image-name" . | quote }}
          imagePullPolicy: {{ .pullPolicy | default "IfNotPresent" | quote }}
          {{- end }}
          {{- if .Values.messageProducerOverride.enabled }}
          envFrom:
            - configMapRef:
                name: message-producer-override-loantransaction
          {{- end }}
          env:
            - name: KEYCLOAK_ENABLED
              value: {{ .Values.global.environment.keycloak.enabled | quote }}
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER-URI
              value: {{ tpl .Values.global.environment.spring.security.oauth2.resourceserver.jwt.issuer_uri . | quote }}
            - name: SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK-SET-URI
              value: {{ tpl .Values.global.environment.spring.security.oauth2.resourceserver.jwt.jwk_set_uri . | quote }}
            - name: KEYCLOAK_AUTH-SERVER-URL
              value: {{ tpl .Values.global.environment.keycloak.auth_server_url_internal . }}
            - name: SPRING_DATASOURCE_URL
              value: {{ tpl .Values.services.loantransactionapi.environment.spring.datasource.url . }}
            - name: SPRING_DATASOURCE_USERNAME
              value: {{ .Values.services.loantransactionapi.environment.spring.datasource.username }}
            - name: SPRING_DATASOURCE_PASSWORD
              {{- if .Values.services.loantransactionapi.environment.spring.datasource.password }}
              value: {{ .Values.services.loantransactionapi.environment.spring.datasource.password }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: loantransactionapi-datasource-secret
                  key: password
              {{- end }}
            - name: SPRING_LIQUIBASE_USER
              value: {{ .Values.services.loantransactionapi.environment.spring.liquibase.username }}
            - name: SPRING_LIQUIBASE_PASSWORD
              {{- if .Values.services.loantransactionapi.environment.spring.liquibase.password }}
              value: {{ .Values.services.loantransactionapi.environment.spring.liquibase.password }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: loantransactionapi-liquibase-secret
                  key: password
              {{- end }}
            - name: SPRING_LIQUIBASE_CONTEXTS
              value: {{ tpl .Values.services.initdb.config.loantransactionapi.spring.liquibase.contexts . }}
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 1
          resources:
            requests:
              memory: {{ .Values.resources.apis.requests.memory }}
              cpu: {{ .Values.resources.apis.requests.cpu }}            
            limits:
              memory: {{ .Values.resources.apis.limits.memory }} 
              cpu: {{ .Values.resources.apis.limits.cpu }}
status: {}
