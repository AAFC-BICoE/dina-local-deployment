{{- if or .Values.global.environment.config.db_init.restore_db .Values.global.environment.config.db_init.reset_users }}
apiVersion: batch/v1
kind: Job
metadata:
  name: db-restore-job
spec:
  template:
    spec:
      initContainers:
        - name: check-db
          image: {{ .Values.services.initdb.image }}
          command:
            - sh
            - -c
            - until pg_isready -h {{ tpl .Values.services.initdb.environment.POSTGRES_HOST . }}; do echo waiting for database; sleep 10; done;
          resources:
            requests:
              memory: {{ .Values.resources.initcontainers.requests.memory }}
              cpu: {{ .Values.resources.initcontainers.requests.cpu }}            
            limits:
              memory: {{ .Values.resources.initcontainers.limits.memory }} 
              cpu: {{ .Values.resources.initcontainers.limits.cpu }}
        {{- if .Values.global.environment.git.host }}
        - name: mount-db-restore
          image: alpine/git
          command:
            - sh
            - -c
            - |
              cd /mnt/data
              git clone https://$(cat /mnt/secrets/git-host)/$(cat /mnt/secrets/git-repo)/$(cat /mnt/secrets/git-project).git
              cd $(cat /mnt/secrets/git-project)
              git lfs pull
              sql_file=$(find . -name "*.sql" -print -quit)
              if [ -n "$sql_file" ]; then
                cat "$sql_file" | base64 > /mnt/data/sql_dump.sql.b64
              else
                echo "No .sql file found."
              fi
          volumeMounts:
            - name: emptydir-volume
              mountPath: /mnt/data
            - name: git-secret-volume
              mountPath: /mnt/secrets
              readOnly: true
        {{- end}}
      containers:
        - name: restore-db
          image: {{ .Values.services.initdb.image }}
          env:
          # Database Connection:
          - name: POSTGRES_HOST
            value: {{ tpl .Values.services.initdb.environment.POSTGRES_HOST . }}
          - name: POSTGRES_DB
            value: {{ tpl .Values.services.initdb.environment.POSTGRES_DB . }}
          - name: POSTGRES_USER
            value: {{ tpl .Values.services.initdb.environment.POSTGRES_USER . }}
          - name: POSTGRES_PASSWORD
            {{- if not .Values.global.environment.config.dina_db.db_password }}
            valueFrom:
              secretKeyRef:
                name: dina-db-secret
                key: password
            {{- else }}
            value: {{ tpl .Values.services.initdb.environment.POSTGRES_PASSWORD . }}
            {{- end }}

          # Database Restore
          - name: RESTORE_DB
            value: "{{ if .Values.services.initdb.environment.RESTORE_DB }}true{{ else }}false{{ end }}"
          - name: DB_DUMP_FILE_PATH
            value: {{ tpl .Values.services.initdb.environment.DB_DUMP_FILE_PATH . }}

          # Reset users / Generate Databases
          - name: RESET_USERS
            value: "{{ if .Values.services.initdb.environment.RESET_USERS }}true{{ else }}false{{ end }}"
          - name: DINA_DB
            value: {{ tpl .Values.services.initdb.environment.DINA_DB . }}
          - name: DB_USER
            value: {{ tpl .Values.services.initdb.environment.DB_USER . }}
          - name: DB_PASSWORD
            value: {{ tpl .Values.services.initdb.environment.DB_PASSWORD . }}

          # Agent API (Defined in Values.services.initdb.config.agentapi)
          - name: MIGRATION_USER_agent
            value: {{ tpl .Values.services.initdb.config.agentapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_agent
            {{- if .Values.services.agentapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.agentapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: agentapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_agent
            value: {{ .Values.services.agentapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_agent
            {{- if .Values.services.agentapi.environment.spring.datasource.password }}
            value: {{ .Values.services.agentapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: agentapi-datasource-secret
                key: password
            {{- end }}

          # Collection API (Defined in Values.services.initdb.config.collectionapi)
          - name: MIGRATION_USER_collection
            value: {{ tpl .Values.services.initdb.config.collectionapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_collection
            {{- if .Values.services.collectionapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.collectionapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: collectionapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_collection
            value: {{ .Values.services.collectionapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_collection
            {{- if .Values.services.collectionapi.environment.spring.datasource.password }}
            value: {{ .Values.services.collectionapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: collectionapi-datasource-secret
                key: password
            {{- end }}
          - name: PG_EXTENSION_collection
            value: {{ .Values.services.initdb.config.collectionapi.PG_EXTENSION_collection }}

          # Dina User API (Defined in Values.services.initdb.config.dinauserapi)
          - name: MIGRATION_USER_dina_user
            value: {{ tpl .Values.services.initdb.config.dinauserapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_dina_user
            {{- if .Values.services.dinauserapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.dinauserapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: dinauserapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_dina_user
            value: {{ .Values.services.dinauserapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_dina_user
            {{- if .Values.services.dinauserapi.environment.spring.datasource.password }}
            value: {{ .Values.services.dinauserapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: dinauserapi-datasource-secret
                key: password
            {{- end }}
          
          # DINA Export API (Defined in Values.services.initdb.config.dinaexportapi)
          - name: MIGRATION_USER_export
            value: {{ tpl .Values.services.initdb.config.dinaexportapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_export
            {{- if .Values.services.dinaexportapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.dinaexportapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: dinaexportapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_export
            value: {{ .Values.services.dinaexportapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_export
            {{- if .Values.services.dinaexportapi.environment.spring.datasource.password }}
            value: {{ .Values.services.dinaexportapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: dinaexportapi-datasource-secret
                key: password
            {{- end }}

          # Loan and Transaction API (Defined in Values.services.initdb.config.loantransactionapi)
          - name: MIGRATION_USER_loan_transaction
            value: {{ tpl .Values.services.initdb.config.loantransactionapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_loan_transaction
            {{- if .Values.services.loantransactionapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.loantransactionapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: loantransactionapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_loan_transaction
            value: {{ .Values.services.loantransactionapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_loan_transaction
            {{- if .Values.services.loantransactionapi.environment.spring.datasource.password }}
            value: {{ .Values.services.loantransactionapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: loantransactionapi-datasource-secret
                key: password
            {{- end }}

          # Object Store API (Defined in Values.services.initdb.config.objectstoreapi)
          - name: MIGRATION_USER_object_store
            value: {{ tpl .Values.services.initdb.config.objectstoreapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_object_store
            {{- if .Values.services.objectstoreapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.objectstoreapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: objectstoreapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_object_store
            value: {{ .Values.services.objectstoreapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_object_store
            {{- if .Values.services.objectstoreapi.environment.spring.datasource.password }}
            value: {{ .Values.services.objectstoreapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: objectstoreapi-datasource-secret
                key: password
            {{- end }}

          # SeqDB API (Defined in Values.services.initdb.config.seqdbapi)
          - name: MIGRATION_USER_seqdb
            value: {{ tpl .Values.services.initdb.config.seqdbapi.spring.liquibase.username . }}
          - name: MIGRATION_USER_PW_seqdb
            {{- if .Values.services.seqdbapi.environment.spring.liquibase.password }}
            value: {{ .Values.services.seqdbapi.environment.spring.liquibase.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: seqdbapi-liquibase-secret
                key: password
            {{- end }}
          - name: WEB_USER_seqdb
            value: {{ .Values.services.seqdbapi.environment.spring.datasource.username }}
          - name: WEB_USER_PW_seqdb
            {{- if .Values.services.seqdbapi.environment.spring.datasource.password }}
            value: {{ .Values.services.seqdbapi.environment.spring.datasource.password }}
            {{- else }}
            valueFrom:
              secretKeyRef:
                name: seqdbapi-datasource-secret
                key: password
            {{- end }}
          - name: PG_EXTENSION_seqdb
            value: {{ .Values.services.initdb.config.seqdbapi.PG_EXTENSION_collection }}
          volumeMounts:
            - name: emptydir-volume
              mountPath: /mnt/data/
          resources:
            requests:
              memory: {{ .Values.resources.initcontainers.requests.memory }}
              cpu: {{ .Values.resources.initcontainers.requests.cpu }}            
            limits:
              memory: {{ .Values.resources.initcontainers.limits.memory }} 
              cpu: {{ .Values.resources.initcontainers.limits.cpu }}
      volumes:
        - name: emptydir-volume
          emptyDir: {}
        - name: git-secret-volume
          secret:
            secretName: git-secret
      restartPolicy: Never
{{- end }}
